amcl:
  ros__parameters:
    alpha1: 0.2
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.2
    alpha5: 0.2
    base_frame_id: "mst110cr_2/base_link"
    beam_skip_distance: 0.5
    beam_skip_error_threshold: 0.9
    beam_skip_threshold: 0.3
    do_beamskip: false
    global_frame_id: "map"
    lambda_short: 0.1
    laser_likelihood_max_dist: 2.0
    laser_max_range: 100.0
    laser_min_range: -1.0
    laser_model_type: "likelihood_field"
    max_beams: 60
    max_particles: 2000
    min_particles: 500
    odom_frame_id: "mst110cr_2/odom"
    pf_err: 0.05
    pf_z: 0.99
    recovery_alpha_fast: 0.0
    recovery_alpha_slow: 0.0
    resample_interval: 1
    robot_model_type: "nav2_amcl::DifferentialMotionModel"
    save_pose_rate: 0.5
    sigma_hit: 0.2
    tf_broadcast: true
    transform_tolerance: 1.0
    update_min_a: 0.2
    update_min_d: 0.25
    z_hit: 0.5
    z_max: 0.05
    z_rand: 0.5
    z_short: 0.05
    scan_topic: scan


bt_navigator:
  ros__parameters:
    use_sim_time: False
    global_frame: map
    robot_base_frame: mst110cr_2/base_link
    odom_topic: /mst110cr_2/odom

    default_nav_to_pose_bt_xml: "mst110cr_navigation/params/mst110cr_navigate_to_pose_w_replanning_and_recovery.xml"
    default_nav_to_pose_bt_xml: "/home/nvidia/ros2_ws/install/mst110cr_navigation/share/mst110cr_navigation/params/mst110cr_navigate_to_pose_w_replanning_and_recovery.xml"
    # default_nav_through_poses_bt_xml: "/mst10cr_navigation/params/mst110cr_navigate_through_poses_w_replanning_and_recovery.xml"

    bt_loop_duration: 100
    default_server_timeout: 20
    # 'default_nav_through_poses_bt_xml' and 'default_nav_to_pose_bt_xml' are use defaults:
    # nav2_bt_navigator/navigate_to_pose_w_replanning_and_recovery.xml
    # nav2_bt_navigator/navigate_through_poses_w_replanning_and_recovery.xml
    # They can be set here or via a RewrittenYaml remap from a parent launch file to Nav2.
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_assisted_teleop_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node
      - logger_node
      - within_goal_radius_node

bt_navigator_navigate_through_poses_rclcpp_node:
  ros__parameters:
    use_sim_time: False

bt_navigator_navigate_to_pose_rclcpp_node:
  ros__parameters:
    use_sim_time: False

controller_server:
  ros__parameters:
    use_sim_time: False
    controller_frequency: 10.0
    min_x_velocity_threshold: 0.002
    # min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.01
    failure_tolerance: 0.3
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"] # "precise_goal_checker"
    controller_plugins: ["RPP", "RSC", "FollowPath"]
    odom_topic: /mst110cr_2/odom_pose

    # Progress checker parameters
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0
    # Goal checker parameters
    #precise_goal_checker:
    #  plugin: "nav2_controller::SimpleGoalChecker"
    #  xy_goal_tolerance: 0.25
    #  yaw_goal_tolerance: 0.25
    #  stateful: True
    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 1.0
      yaw_goal_tolerance: 0.15


    RSC:
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      angular_dist_threshold: 10.0 #[rad]
      forward_sampling_distance: 1.0
      angular_disengage_threshold: 0.3925
      rotate_to_heading_angular_vel: 0.5
      max_angular_accel: 2.0
      simulate_ahead_time: 1.0
      rotate_to_goal_heading: true

      use_velocity_scaled_lookahead_dist: true
      # --- if use_velocity_scaled_lookahead_dist is true ---
      lookahead_time: 2.2              # （据え置き）終盤で見過ぎない
      min_lookahead_dist: 2.0          # 1.2 → 1.4
      max_lookahead_dist: 3.0          # 3.5 → 3.0

      # --- if use_velocity_scaled_lookahead_dist is false ---
      #lookahead_dist: 1.0

      use_fixed_curvature_lookahead: true
      # --- if use_fixed_curvature_lookahead is true ---
      interpolate_curvature_after_goal: false


      curvature_lookahead_dist: 2.0    # 2.2 → 2.6
      desired_linear_vel: 1.0
      approach_velocity_scaling_dist: 3.5  # 1.2 → 2.0（早め減速）
      min_approach_linear_velocity: 0.25   # 0.15 → 0.25（低速すぎ蛇行を防止）
 
      use_rotate_to_heading: false
      # --- if use_rotate_to_heading is true ---
      # rotate_to_heading_angular_vel: 0.5
      # rotate_to_heading_min_angle: 0.5
      # max_angular_accel: 2.0

      # --- use_rotate_to_heading is false ---
      allow_reversing: true


      use_regulated_linear_velocity_scaling: true
      # --- if use_regulated_linear_velocity_scaling is true ---
      regulated_linear_scaling_min_radius: 3.8   # 1.8 → 2.2（早めに減速）

      regulated_linear_scaling_min_speed: 0.05   # 0.25 → 0.15（低速で粘る）
 
      use_cost_regulated_linear_velocity_scaling: true
      # --- if use_cost_regulated_linear_velocity_scaling is true ---
      cost_scaling_dist: 0.5
      cost_scaling_gain: 1.0
 
      use_collision_detection: true
      # --- if use_collision_detection is true ---
      max_allowed_time_to_collision_up_to_carrot: 2.0  # 1.5 → 2.0（ON/OFFの細かい切替を緩和）
 
      max_robot_pose_search_dist: 1.0  # 3.0 → 2.0（近傍点を確実に拾って急舵を防ぐ）
      transform_tolerance: 0.2
      stateful: true



    RPP:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"

      use_velocity_scaled_lookahead_dist: true
      # --- if use_velocity_scaled_lookahead_dist is true ---
      lookahead_time: 2.2              # （据え置き）終盤で見過ぎない
      min_lookahead_dist: 2.0          # 1.2 → 1.4
      max_lookahead_dist: 3.0          # 3.5 → 3.0

      # --- if use_velocity_scaled_lookahead_dist is false ---
      #lookahead_dist: 1.0

      use_fixed_curvature_lookahead: true
      # --- if use_fixed_curvature_lookahead is true ---
      interpolate_curvature_after_goal: false


      curvature_lookahead_dist: 2.0    # 2.2 → 2.6
      desired_linear_vel: 1.0
      approach_velocity_scaling_dist: 3.5  # 1.2 → 2.0（早め減速）
      min_approach_linear_velocity: 0.25   # 0.15 → 0.25（低速すぎ蛇行を防止）
 
      use_rotate_to_heading: false

      # --- if use_rotate_to_heading is true ---
      # rotate_to_heading_angular_vel: 0.5
      # rotate_to_heading_min_angle: 0.5
      # max_angular_accel: 2.0

      # --- use_rotate_to_heading is false ---
      allow_reversing: true


      use_regulated_linear_velocity_scaling: true
      # --- if use_regulated_linear_velocity_scaling is true ---
      regulated_linear_scaling_min_radius: 3.8   # 1.8 → 2.2（早めに減速）

      regulated_linear_scaling_min_speed: 0.05   # 0.25 → 0.15（低速で粘る）
 
      use_cost_regulated_linear_velocity_scaling: true
      # --- if use_cost_regulated_linear_velocity_scaling is true ---
      cost_scaling_dist: 0.5
      cost_scaling_gain: 1.0
 
      use_collision_detection: true
      # --- if use_collision_detection is true ---
      max_allowed_time_to_collision_up_to_carrot: 2.0  # 1.5 → 2.0（ON/OFFの細かい切替を緩和）
 
      max_robot_pose_search_dist: 1.0  # 3.0 → 2.0（近傍点を確実に拾って急舵を防ぐ）
      transform_tolerance: 0.2
      stateful: true



    FollowPath:
      plugin: "dwb_core::DWBLocalPlanner"
      debug_trajectory_details: false
      min_vel_x: -3.0
      min_vel_y: 0.0
      max_vel_x: 3.0
      max_vel_y: 0.0
      max_vel_theta: 1.0
      min_speed_xy: -2.0
      max_speed_xy: 2.0
      min_speed_theta: -1.0
      max_speed_theta: 1.0
      acc_lim_x: 3.0 
      acc_lim_y: 0.0
      acc_lim_theta: 3.1
      decel_lim_x: -2.5
      decel_lim_y: 0.0
      decel_lim_theta: -3.2
      vx_samples: 25
      vy_samples: 5
      vtheta_samples: 25
      # sim_time: 30.0
      sim_time: 1.7
      linear_granularity: 0.05
      angular_granularity: 0.025
      transform_tolerance: 0.2
      xy_goal_tolerance: 0.25
      trans_stopped_velocity: 0.25
      short_circuit_trajectory_evaluation: True
      stateful: True
      critics: ["RotateToGoal", "Oscillation", "BaseObstacle", "GoalAlign", "PathAlign", "PathDist", "GoalDist"]
      BaseObstacle.scale: 0.02
      PathAlign.scale: 32.0
      PathAlign.forward_point_distance: 0.1
      GoalAlign.scale: 24.0
      GoalAlign.forward_point_distance: 0.1
      PathDist.scale: 32.0
      GoalDist.scale: 24.0
      RotateToGoal.scale: 32.0
      RotateToGoal.slowing_factor: 5.0
      RotateToGoal.lookahead_time: -1.0

local_costmap:
  local_costmap:
    ros__parameters:
      #Costmap2D paraneters
      always_send_full_costmap: True
      use_sim_time: False
      footprint_padding: 0.5
      global_frame: mst110cr_2/odom
      height: 20
      width: 20
      observation_sources: scan
      publish_frequency: 10.0
      resolution: 0.05
      robot_base_frame: mst110cr_2/base_link
      rolling_window: true
      transform_tolerance: 0.5
      update_frequency: 10.0
      plugins: ["static_layer", "inflation_layer"]
      
      # Obstacle Layer parameters
      # obstacle_layer:
      #   plugin: "nav2_costmap_2d::ObstacleLayer"
      #   enabled: True
      #   observation_sources: scan
      #   scan:
      #     topic: front/scan
      #     max_obstacle_height: 2.0
      #     clearing: True
      #     marking: True
      #     data_type: "LaserScan"
      #     raytrace_max_range: 3.0
      #     raytrace_min_range: 0.0
      #     obstacle_max_range: 2.5
      #     obstacle_min_range: 0.0
      #   sensor_frame: front_laser
      
      #Static Layer parameters
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
        always_send_full_costmap: True

      # Voxel Layer parameters
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      
      # Infration Layer parameters
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.60

global_costmap:
  global_costmap:
    ros__parameters:
      #Costmap2D paraneters
      always_send_full_costmap: True
      use_sim_time: False
      footprint_padding: 0.5
      global_frame: map
      height: 100
      width: 100
      observation_sources: scan
      origin_x: -20.0
      origin_y: -20.0
      publish_frequency: 10.0
      resolution: 0.1
      robot_base_frame: mst110cr_2/base_link
      rolling_window: false
      transform_tolerance: 0.5
      update_frequency: 10.0
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      
      # Obstacle Layer parameters
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: front/scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
        sensor_frame: front_laser
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      # Voxel Layer parameters
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        publish_voxel_map: false
        origin_z: 0.0
        z_resolution: 1
        z_voxels: 2
      
      # Infration Layer parameters
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.6

map_server:
  ros__parameters:
    use_sim_time: False
    # Overridden in launch by the "map" launch configuration or provided default value.
    # To use in yaml, remove the default "map" value in the tb3_simulation_launch.py file & provide full path to map below.
    yaml_filename: ""

map_saver:
  ros__parameters:
    use_sim_time: False
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True
    

planner_server:
  ros__parameters:
    expected_planner_frequency: 5.0
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_theta_star_planner/ThetaStarPlanner"
      w_euc_cost: 1.0
      w_traversal_cost: 0.05
      # w_traversal_cost: 4.0


    # planner_plugins: ["GridBased"]

    # GridBased:
    #   plugin: "nav2_smac_planner/SmacPlannerHybrid"
    #   allow_unknown: true
    #   max_iterations: 100000
    #   motion_model_for_search: "REEDS_SHEPP" 
    #   angle_quantization_bins: 64
    #   reverse_penalty: 1.0
    #   non_straight_penalty: 10000.0
    #   change_penalty: 5.0
    #   cost_penalty: 2.0
    #   goal_heading_mode: "ALL_DIRECTION"
    #   smooth_path: false

    #   max_planning_time: 2.0

    #   analytic_expansion_ratio: 10.0
    #   analytic_expansion_max_cost_override: true


    #   smoother:
    #     max_iterations: 100
    #     w_smooth: 0.4
    #     w_data: 0.2
    #     tolerance: 1.0e-10
    #     do_refinement: true
    #     refinement_num: 2

# Theta star planner (Global Path Planner)
# planner_server:
#   ros__parameters:
#     expected_planner_frequency: 20.0
#     planner_plugins: ["GridBased"]
#     GridBased:
#       plugin: "nav2_theta_star_planner/ThetaStarPlanner"
#       w_euc_cost: 1.0
#       w_traversal_cost: 4.0

# NavFn planner (Global Path Planner)
# planner_server:
#   ros__parameters:
#     expected_planner_frequency: 4.0
#     use_sim_time: False
#     planner_plugins: ["GridBased"]
#     GridBased:
#       plugin: "nav2_navfn_planner/NavfnPlanner"
#       tolerance: 0.5
#       use_astar: false
#       allow_unknown: true

smoother_server:
  ros__parameters:
    use_sim_time: False
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: True

behavior_server:
  ros__parameters:
    local_costmap_topic: local_costmap/costmap_raw
    global_costmap_topic: global_costmap/costmap_raw
    local_footprint_topic: local_costmap/published_footprint
    global_footprint_topic: global_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "drive_on_heading", "assisted_teleop", "wait"]
    spin:
      plugin: "nav2_behaviors/Spin"
    backup:
      plugin: "nav2_behaviors/BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors/DriveOnHeading"
    wait:
      plugin: "nav2_behaviors/Wait"
    assisted_teleop:
      plugin: "nav2_behaviors/AssistedTeleop"
    local_frame: mst110cr_2/odom
    global_frame: map
    robot_base_frame: mst110cr_2/base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2

robot_state_publisher:
  ros__parameters:
    use_sim_time: False

waypoint_follower:
  ros__parameters:
    use_sim_time: False
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

velocity_smoother:
  ros__parameters:
    smoothing_frequency: 10.0
    scale_velocities: False
    feedback: "OPEN_LOOP"
    # max_velocity: [1.0, 0.0, 0.3490658504]
    # min_velocity: [-1.0, 0.0, -0.3490658504]
    max_velocity: [1.0, 0.0, 0.5]
    min_velocity: [-1.0, 0.0, -0.5]
    max_accel: [2.0, 0.0, 1.0]
    max_decel: [-2.0, 0.0, -1.0]
    odom_topic: /mst110cr_2/odom_pose
    odom_duration: 2.0
    deadband_velocity: [0.01, 0.0, 0.01]
    velocity_timeout: 0.5